# Readme.MD

After cloning the repository, run `npm install --all` to gather dependencies, then run `npm start` to view the site!

# Note!

This repo is a compilation of visualizations for my senior thesis. It is still in the works! Check back for a more finished product June '22 :)
